import React from "react";
import { Button, Text, View } from 'react-native';
import { AuthenticationProps } from "../../types/navigation-types";

export const AuthenticationPage = ({navigation}: AuthenticationProps) => {

    return (
        <View>
            <Text>Authentication Page</Text>
        </View>
    )
}
