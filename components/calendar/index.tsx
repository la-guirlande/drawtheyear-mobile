import React from "react";
import { Button, Text, View } from 'react-native';
import { CalendarProps } from "../../types/navigation-types";

export const CalendarPage = ({navigation}: CalendarProps) => {

    return (
        <View>
            <Text>Calendar Page</Text>
        </View>
    )
}
